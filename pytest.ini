[pytest]
markers =
    integration: marks tests that require external resources (database, live API) (deselect with -m "not integration")
pythonpath = .
addopts = -m "not integration" --cov=src --cov-report=term-missing --cov-fail-under=85 --cov-config=.coveragerc
