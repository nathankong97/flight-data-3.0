# Flight Data 3.0 — example environment configuration

# Option A: provide a complete PostgreSQL DSN in DATABASE_URL
#DATABASE_URL=postgresql://postgres:dev@127.0.0.1:5432/postgres

# Option B: provide discrete components; load_config() will build DATABASE_URL
#HOST=127.0.0.1
#USER=postgres
#PASSWORD=dev
#DB=postgres
#PORT=5432

# Logging configuration
LOG_DIR=logs
LOG_LEVEL=INFO

# Optional: application name used in log file naming
APP_NAME=flight-data

# (Optional, tests): override the airport used by API->transform integration test
#TEST_AIRPORT_CODE=HND

# Telegram alerts (optional)
# Set these to enable Telegram alerting and integration tests.
#TELEGRAM_BOT_TOKEN=123456:abcDEF-your-bot-token
#TELEGRAM_CHAT_ID=-1001234567890
#TELEGRAM_PARSE_MODE=HTML

# resilient fetch + diagnostics
# Enable one final direct/local attempt after proxy retries (transport errors only)
#FD_DIRECT_FALLBACK=false
# Allow direct fallback for 429 responses (disabled by default)
#FD_DIRECT_FALLBACK_ON_429=false
# Split attempts: how many via proxy vs direct (if unset, uses retry_attempts - direct)
#FD_RETRY_ATTEMPTS_PROXY=2
#FD_RETRY_ATTEMPTS_DIRECT=1
# Mark run as DEGREDADED when give-ups exceed this threshold
#FD_DEGRADED_FAIL_THRESHOLD=1
